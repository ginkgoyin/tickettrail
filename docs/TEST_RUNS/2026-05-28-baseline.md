# Baseline Test Run - 2026-05-28

## 1. Purpose

Record the current baseline behavior of TicketTrail before new bug fixes, feature work, navigation changes, or refactors.

## 2. Environment

- OS: Microsoft Windows NT 10.0.19045.0
- Node version: v24.15.0
- npm version: 11.12.1
- Rust version: rustc 1.95.0 (59807616e 2026-04-14)
- Cargo version: cargo 1.95.0 (f2d3ce0bd 2026-03-21)
- Tauri/dev mode or release mode: Dev mode verification attempted via `npm.cmd run tauri:dev`; release mode not verified in this run
- Test date: 2026-05-28
- Tester: Codex

## 3. Commands To Run

### [x] `npm run test`
- Result: Pass
- Notes: Ran successfully via `npm.cmd run test`. Vitest reported 3 test files and 11 tests passed.
- Error output summary: none

### [x] `cargo test --manifest-path src-tauri/Cargo.toml`
- Result: Pass
- Notes: Rust tests completed successfully. 4 unit tests passed in `db.rs`.
- Error output summary: none

### [x] `npm run build`
- Result: Pass
- Notes: Ran successfully via `npm.cmd run build`. Frontend build completed and generated production assets.
- Error output summary: none; build emitted a chunk-size warning for `maplibre-vendor`

### [x] `npm run tauri:dev`
- Result: Pass with manual verification still needed
- Notes: Ran via `npm.cmd run tauri:dev`. Output showed Vite ready on `http://localhost:1420`, Cargo dev build finished, and `target\\debug\\tickettrail.exe` was launched. Command timed out because it is a long-running dev process, which is expected. Desktop window visibility still needs manual confirmation.
- Error output summary: no startup error observed before timeout; timeout was due to the long-running dev process

## 4. Manual Desktop Checklist

### 4.1 App startup

- [ ] Not tested
- [x] Pass
- [ ] Fail
- [ ] Blocked
- Notes: The app did open initially before the later blank-screen bug. After a record was added, the UI became completely blank and unusable.

### 4.2 Ticket CRUD

- [ ] Not tested
- [ ] Pass
- [x] Fail
- [ ] Blocked
- Notes: Tester was able to start the add flow and add one record, but after saving/finishing the add flow the entire UI became blank. Edit, delete, status update, and persistence after restart could not be fully verified because the app became unusable.

### 4.3 Search / filter / sort

- [ ] Not tested
- [ ] Pass
- [ ] Fail
- [x] Blocked
- Notes: Further manual verification was blocked after the blank-screen bug triggered during record creation.

### 4.4 Ticket detail and journey/segment behavior

- [ ] Not tested
- [ ] Pass
- [ ] Fail
- [x] Blocked
- Notes: Further manual verification was blocked after the UI became blank following record creation.

### 4.5 Import and OCR

- [x] Not tested
- [ ] Pass
- [ ] Fail
- [ ] Blocked
- Notes: Not manually verified in this baseline run before the app became unusable.

### 4.6 Attachments

- [x] Not tested
- [ ] Pass
- [ ] Fail
- [ ] Blocked
- Notes: Not manually verified in this baseline run before the app became unusable.

### 4.7 Map and visualization

- [ ] Not tested
- [ ] Pass
- [ ] Fail
- [x] Blocked
- Notes: The tester reported the right-side interface stacks too many unrelated features into one screen. Map behavior after record creation could not be verified because the UI became blank.

### 4.8 Statistics

- [ ] Not tested
- [ ] Pass
- [ ] Fail
- [x] Blocked
- Notes: Statistics behavior after adding records could not be verified because the app became blank after the add flow.

### 4.9 Backup / restore / archive import-export

- [x] Not tested
- [ ] Pass
- [ ] Fail
- [ ] Blocked
- Notes: Not manually verified in this baseline run before the app became unusable.

### 4.10 Ticket stub and data export

- [x] Not tested
- [ ] Pass
- [ ] Fail
- [ ] Blocked
- Notes: Not manually verified in this baseline run before the app became unusable.

### 4.11 Windows-specific checks

- [x] Not tested
- [ ] Pass
- [ ] Fail
- [ ] Blocked
- Notes: Not manually verified in this baseline run.

## 5. Issues Found

| ID | Area | Severity | Description | Steps to reproduce | Expected | Actual | Status |
|----|------|----------|-------------|--------------------|----------|--------|--------|
| BL-001 | Build | Low | Production build emits a large chunk warning for `maplibre-vendor` | Run `npm.cmd run build` | Build completes without notable bundle-size warnings | Build succeeds, but Vite warns that a chunk is larger than 500 kB after minification | Open |
| BL-002 | Desktop startup | Medium | `npm run tauri:dev` startup sequence succeeded, but desktop window visibility was not manually confirmed in this run | Run `npm.cmd run tauri:dev` and allow startup to continue | Dev startup is confirmed and app window is visibly usable | CLI output shows Vite ready, Cargo finished, and `tickettrail.exe` launched, but no manual window confirmation was recorded | Needs verification |
| BL-003 | Ticket CRUD / App Stability | Critical | Adding a record causes the entire app page to become blank | Open app, add a new record, finish the add flow | Record is saved and the app returns to a usable ticket list/detail view | The page became completely blank on the original baseline run. After the first fix attempt, the ErrorBoundary caught a follow-up runtime error: `Cannot read properties of undefined (reading 'getLayer')`. A second RouteMap lifecycle fix has now been attempted and still needs manual verification. | Fix attempted / needs manual verification |
| BL-004 | Navigation / Layout | High | Left sidebar menu is not persistently visible and requires scrolling to the bottom to see options | Open the current workspace and inspect the sidebar area during normal use | Sidebar remains visible/sticky and accessible | Sidebar options are only visible after scrolling | Open |
| BL-005 | Navigation | High | Left sidebar buttons do not respond when clicked | Open app and click the left sidebar buttons | Sidebar buttons switch sections or pages | Clicking buttons has no visible effect | Open |
| BL-006 | UX Structure | High | Main interface stacks too many unrelated features on one page | Open app and inspect the main workspace before the blank-screen bug occurs | Features should be categorized into Home, flight tickets, train tickets, import/OCR, backup/export, statistics, and other logical sections | Most features are piled into a single workspace | Open |
| BL-007 | Import / OCR UX | Medium | OCR appears as a permanent main-page section instead of being part of the add-new ticket flow | Open app and inspect the current import/OCR area | OCR should be accessible from the add-new ticket window/modal | OCR/import functionality is displayed as part of the stacked main page | Open |
| BL-008 | Product / Information Architecture | High | The app needs a full feature categorization and navigation redesign before further UI expansion | Review current navigation and workspace structure during use | The product should have clear functional areas and user flows | Current UI lacks clear feature grouping | Open |

## 6. Summary

- Overall result: Automated baseline passed, and targeted BL-003 follow-up fixes have been attempted in code. Manual desktop verification is still required before the blank-screen issue can be considered resolved.
- Highest-risk failures: `BL-003` still needs manual create-ticket retesting after the follow-up `getLayer` runtime error; navigation/UX structure issues `BL-004` to `BL-008` remain open.
- Recommended next fixes: Manually retest the create-ticket flow first. If BL-003 still reproduces, capture the first runtime/console error and continue with another narrow fix. After BL-003 is verified, continue with the documented navigation/UX planning work.
- Follow-up docs to update: `docs/TASKS.md`, `docs/PROJECT_STATUS.md`, and possibly a future `docs/UX_STRUCTURE.md` or `docs/UX_REDESIGN_PLAN.md`.
- BL-003 fix attempt notes: The first fix added defensive rendering guards around ticket detail/map/stub rendering, a minimal React error boundary, and safer list/dashboard formatting. That exposed a follow-up runtime error, `Cannot read properties of undefined (reading 'getLayer')`, during map rendering. A second fix now hardens `RouteMap` against map initialization/cleanup races and missing map instances. Commands run for this follow-up fix attempt: `npm run test`, `npm run build`.
- Sidebar/navigation note: A later local-section navigation step made the sidebar sticky/persistently visible and made sidebar buttons switch local sections. The BL-003 add-ticket path should still be retested after navigation changes if it has not been rechecked manually in the new layout.
- Page wrapper verification note: `HomePage` and `TicketsPage` wrappers were introduced as the first minimal page-level composition step. The user manually verified `Overview` and `Tickets`, successfully added both a flight ticket and a train ticket after the extraction, switched ticket detail successfully, and observed no `ErrorBoundary`, blank screen, or terminal error during this verification.
- `NAV-003` verification note: The user manually verified that switching sidebar sections now scrolls the main workspace/content area back to the top. Sidebar stickiness and section switching still worked, the add-ticket flow still worked, and no blank screen or `ErrorBoundary` appeared during this check.
- `UX-003` verification note: The user manually verified that `Overview` no longer repeats selected-ticket preview/detail/stub/attachment panels, `Tickets` now uses clearer selected-ticket labels, `Exports` now uses export-specific preview labels, and no duplicate generic `Preview` panels remained in the checked flow.
- `UX-005` verification note: The user manually verified the current desktop-first Tickets workflow refinement. `Add ticket` now opens a modal instead of expanding below the list, the modal has a top-right `X`, the width and field layout are acceptable, the route summary sits near the bottom above Save, decorative eyebrow labels were reduced acceptably, and both flight/train create flows still worked without blank screens or `ErrorBoundary`. Pagination is implemented but still needs future manual verification with more than `20` records.
- Journeys Phase 1 scaffold note: The user manually verified that clicking the sidebar `Journeys` entry opens `Summary` by default, `Summary / List` switching works, Journeys no longer duplicates Tickets single-ticket detail, the scaffold uses safe empty/placeholder states, and no blank screen or `ErrorBoundary` was observed.
- Ticket edit verification note: The user manually verified that ticket detail `Edit` now opens a working edit modal, edit mode no longer shows OCR/import or ticket-type switching, ticket number labels reflect flight/train type, helper chips and mode pills were removed acceptably, fuller ticket information is shown in detail, edited data saves and refreshes the current detail view, and no blank screen or `ErrorBoundary` was observed.
- Location filtering verification note: The user manually verified that add/edit flight forms now show only airport suggestions with airport wording, add/edit train/rail forms now show only station suggestions with station wording, add and edit flows still work, detail refresh after edit still works, and no blank screen or `ErrorBoundary` was observed.
- `FORM-002` verification note: The user manually verified that add-flight tickets default `Cabin / Class` to `Economy`, saving without changes stores `Economy`, add train/rail tickets use a reasonable default, existing tickets keep their current class values during edit, location suggestions remain scoped by ticket type, and no blank screen or `ErrorBoundary` was observed.
- Airport map-resolution verification note: The user manually verified that generated/global airport data is now connected to route-map coordinate resolution, flight tickets using airport codes can resolve coordinates for the map after save, airport-based routes render successfully, and no blank screen or `ErrorBoundary` was observed.
- Map endpoint-alignment verification note: The user manually verified that Overview maps no longer show large labels/popups, Overview and ticket/detail route endpoint dots now align with the exact line endpoints, flight routes using airport coordinates still render correctly, missing-coordinate fallbacks remain safe, and no blank screen or `ErrorBoundary` was observed.
- Map crash-recovery note: After the later low-zoom/world-boundary experiment triggered `Cannot read properties of null (reading '0')`, the unstable MAP-006 / MAP-007 / MAP-008 attempt was rolled back. The user then manually verified that the workspace no longer enters `ErrorBoundary`, Overview and ticket detail maps open again, airport-based flight routes still render, and endpoint alignment / Overview no-label behavior remain stable.
- Terminal-fields verification note: The user manually verified that flight Add/Edit forms now show `Departure terminal` / `Arrival terminal`, terminal values save and reload correctly, ticket detail displays terminals correctly, old tickets without terminal values still work, train/rail forms do not show flight terminal fields, ticket stub preview remains usable, and no blank screen or `ErrorBoundary` was observed.
